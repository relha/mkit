---
---

<section class="testimonials-section" id="testimonials">
  <div class="container">
    <h2 class="section-title">Ce que nos clients disent</h2>
    
    <div class="testimonials-slider-container">
      <button class="slider-nav prev" aria-label="Témoignage précédent">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      
      <div class="testimonials-slider" id="testimonialsSlider">
        <!-- Testimonial 1 -->
        <div class="testimonial-card">
          <div class="video-container" data-video-src="/videos/testimonial1.mp4">
            <div class="video-placeholder">
              <div class="play-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"></circle>
                  <polygon points="10 8 16 12 10 16 10 8"></polygon>
                </svg>
              </div>
            </div>
            <!-- Video will be loaded here on hover -->
          </div>
          <div class="testimonial-content">
            <blockquote>
              "Nous avons réduit notre temps administratif de 40% grâce à l'automatisation IA. Un gain de productivité incroyable !"
            </blockquote>
            <div class="testimonial-author">
              <strong>Marie Dupont</strong>
              <span>Directrice, Agence Marketing Digital</span>
            </div>
          </div>
        </div>
        
        <!-- Testimonial 2 -->
        <div class="testimonial-card">
          <div class="video-container" data-video-src="/videos/testimonial2.mp4">
            <div class="video-placeholder">
              <div class="play-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"></circle>
                  <polygon points="10 8 16 12 10 16 10 8"></polygon>
                </svg>
              </div>
            </div>
            <!-- Video will be loaded here on hover -->
          </div>
          <div class="testimonial-content">
            <blockquote>
              "Notre taux de conversion a augmenté de 28% depuis que nous utilisons l'IA pour personnaliser nos interactions clients."
            </blockquote>
            <div class="testimonial-author">
              <strong>Thomas Laurent</strong>
              <span>Fondateur, E-commerce Mobilier</span>
            </div>
          </div>
        </div>
        
        <!-- Testimonial 3 -->
        <div class="testimonial-card">
          <div class="video-container" data-video-src="/videos/testimonial3.mp4">
            <div class="video-placeholder">
              <div class="play-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"></circle>
                  <polygon points="10 8 16 12 10 16 10 8"></polygon>
                </svg>
              </div>
            </div>
            <!-- Video will be loaded here on hover -->
          </div>
          <div class="testimonial-content">
            <blockquote>
              "L'IA nous a permis d'économiser plus de 15 000€ par an en automatisant notre service client. Le ROI est évident."
            </blockquote>
            <div class="testimonial-author">
              <strong>Sophie Martin</strong>
              <span>CEO, Services Financiers</span>
            </div>
          </div>
        </div>
        
        <!-- Testimonial 4 -->
        <div class="testimonial-card">
          <div class="video-container" data-video-src="/videos/testimonial4.mp4">
            <div class="video-placeholder">
              <div class="play-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"></circle>
                  <polygon points="10 8 16 12 10 16 10 8"></polygon>
                </svg>
              </div>
            </div>
            <!-- Video will be loaded here on hover -->
          </div>
          <div class="testimonial-content">
            <blockquote>
              "Nos équipes ont gagné 12 heures par semaine grâce à l'automatisation des tâches répétitives. C'est un changement radical."
            </blockquote>
            <div class="testimonial-author">
              <strong>Jean Moreau</strong>
              <span>Directeur Opérations, Industrie Manufacturière</span>
            </div>
          </div>
        </div>
      </div>
      
      <button class="slider-nav next" aria-label="Témoignage suivant">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </button>
    </div>
    
    <div class="slider-dots" id="sliderDots">
      <!-- Dots will be generated by JS -->
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const slider = document.getElementById('testimonialsSlider');
    const prevBtn = document.querySelector('.slider-nav.prev');
    const nextBtn = document.querySelector('.slider-nav.next');
    const dotsContainer = document.getElementById('sliderDots');
    
    if (!slider || !prevBtn || !nextBtn || !dotsContainer) return;
    
    const testimonials = slider.querySelectorAll('.testimonial-card');
    const testimonialCount = testimonials.length;
    let currentIndex = 0;
    let isAnimating = false;
    let autoplayInterval;
    let touchStartX = 0;
    let touchEndX = 0;
    
    // Clone testimonials for infinite effect
    testimonials.forEach(testimonial => {
      const clone = testimonial.cloneNode(true);
      slider.appendChild(clone);
    });
    
    // Create dots
    for (let i = 0; i < testimonialCount; i++) {
      const dot = document.createElement('button');
      dot.classList.add('slider-dot');
      dot.setAttribute('aria-label', `Témoignage ${i + 1}`);
      if (i === 0) dot.classList.add('active');
      
      dot.addEventListener('click', () => {
        goToSlide(i);
      });
      
      dotsContainer.appendChild(dot);
    }
    
    const dots = dotsContainer.querySelectorAll('.slider-dot');
    
    // Initialize slider position
    updateSliderPosition();
    
    // Function to update slider position
    function updateSliderPosition(animate = true) {
      const slideWidth = testimonials[0].offsetWidth;
      const newPosition = -currentIndex * slideWidth;
      
      slider.style.transition = animate ? 'transform 0.5s ease' : 'none';
      slider.style.transform = `translateX(${newPosition}px)`;
      
      // Update active dot
      dots.forEach((dot, index) => {
        dot.classList.toggle('active', index === currentIndex % testimonialCount);
      });
      
      // Handle infinite scroll effect
      if (animate) {
        isAnimating = true;
        setTimeout(() => {
          isAnimating = false;
          
          // If we've moved beyond the original set, jump back to the clone without animation
          if (currentIndex >= testimonialCount) {
            currentIndex = currentIndex % testimonialCount;
            updateSliderPosition(false);
          }
          
          // If we've moved before the original set, jump to the end clone without animation
          if (currentIndex < 0) {
            currentIndex = testimonialCount - 1;
            updateSliderPosition(false);
          }
        }, 500);
      }
    }
    
    // Function to go to a specific slide
    function goToSlide(index) {
      if (isAnimating) return;
      currentIndex = index;
      updateSliderPosition();
      resetAutoplay();
    }
    
    // Function to go to the next slide
    function nextSlide() {
      if (isAnimating) return;
      currentIndex++;
      updateSliderPosition();
      resetAutoplay();
    }
    
    // Function to go to the previous slide
    function prevSlide() {
      if (isAnimating) return;
      currentIndex--;
      updateSliderPosition();
      resetAutoplay();
    }
    
    // Set up autoplay
    function startAutoplay() {
      autoplayInterval = setInterval(nextSlide, 5000);
    }
    
    function resetAutoplay() {
      clearInterval(autoplayInterval);
      startAutoplay();
    }
    
    // Event listeners
    nextBtn.addEventListener('click', nextSlide);
    prevBtn.addEventListener('click', prevSlide);
    
    // Keyboard navigation
    document.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') {
        prevSlide();
      } else if (e.key === 'ArrowRight') {
        nextSlide();
      }
    });
    
    // Touch events for swipe
    slider.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });
    
    slider.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });
    
    function handleSwipe() {
      const swipeThreshold = 50;
      const swipeDistance = touchEndX - touchStartX;
      
      if (swipeDistance > swipeThreshold) {
        prevSlide(); // Swipe right
      } else if (swipeDistance < -swipeThreshold) {
        nextSlide(); // Swipe left
      }
    }
    
    // Video hover functionality
    const videoContainers = document.querySelectorAll('.video-container');
    
    videoContainers.forEach(container => {
      container.addEventListener('mouseenter', () => {
        const videoSrc = container.getAttribute('data-video-src');
        if (!videoSrc) return;
        
        // Check if video already exists
        if (container.querySelector('video')) return;
        
        // Create video element
        const video = document.createElement('video');
        video.autoplay = true;
        video.loop = true;
        video.muted = true;
        video.playsInline = true;
        
        const source = document.createElement('source');
        source.src = videoSrc;
        source.type = 'video/mp4';
        
        video.appendChild(source);
        container.appendChild(video);
        
        // Hide placeholder
        const placeholder = container.querySelector('.video-placeholder');
        if (placeholder) {
          placeholder.style.opacity = '0';
        }
      });
    });
    
    // Start autoplay
    startAutoplay();
    
    // Handle window resize
    window.addEventListener('resize', () => {
      updateSliderPosition(false);
    });
  });
</script>

<style>
  .testimonials-section {
    padding: 5rem 0;
    background-color: #f9f9f9;
  }
  
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  
  .section-title {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 3rem;
    color: var(--text-color);
    font-weight: 700;
  }
  
  /* Testimonials slider */
  .testimonials-slider-container {
    position: relative;
    display: flex;
    align-items: center;
    margin-bottom: 2rem;
  }
  
  .testimonials-slider {
    display: flex;
    overflow: hidden;
    width: 100%;
    margin: 0 auto;
  }
  
  .testimonial-card {
    flex: 0 0 100%;
    padding: 1rem;
    box-sizing: border-box;
  }
  
  @media (min-width: 768px) {
    .testimonial-card {
      flex: 0 0 50%;
    }
  }
  
  @media (min-width: 1024px) {
    .testimonial-card {
      flex: 0 0 33.333%;
    }
  }
  
  .testimonial-card-inner {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    height: 100%;
    display: flex;
    flex-direction: column;
  }
  
  /* Video container */
  .video-container {
    position: relative;
    width: 100%;
    height: 0;
    padding-bottom: 56.25%; /* 16:9 aspect ratio */
    background: #eee;
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 1rem;
  }
  
  .video-container video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }
  
  .video-placeholder {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: opacity 0.3s ease;
    z-index: 0;
  }
  
  .play-icon {
    width: 60px;
    height: 60px;
    color: white;
    opacity: 0.8;
  }
  
  /* Testimonial content */
  .testimonial-content {
    padding: 1rem;
    background: white;
    border-radius: 12px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
  }
  
  blockquote {
    font-size: 1.1rem;
    line-height: 1.6;
    color: var(--text-color);
    font-style: italic;
    margin-bottom: 1rem;
    position: relative;
  }
  
  blockquote::before {
    content: '"';
    font-size: 3rem;
    color: var(--primary-color);
    opacity: 0.2;
    position: absolute;
    top: -1rem;
    left: -1rem;
  }
  
  .testimonial-author {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  
  .testimonial-author strong {
    color: var(--text-color);
    font-weight: 600;
  }
  
  .testimonial-author span {
    color: #666;
    font-size: 0.9rem;
  }
  
  /* Navigation buttons */
  .slider-nav {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: white;
    border: none;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    z-index: 2;
    flex-shrink: 0;
  }
  
  .slider-nav:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
  }
  
  .slider-nav:active {
    transform: translateY(0);
  }
  
  .slider-nav svg {
    width: 20px;
    height: 20px;
    color: var(--primary-color);
  }
  
  .slider-nav.prev {
    margin-right: 1rem;
  }
  
  .slider-nav.next {
    margin-left: 1rem;
  }
  
  /* Slider dots */
  .slider-dots {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
  }
  
  .slider-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #ddd;
    border: none;
    padding: 0;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
  }
  
  .slider-dot.active {
    background: var(--primary-color);
    transform: scale(1.2);
  }
  
  /* Responsive adjustments */
  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }
    
    .slider-nav {
      width: 36px;
      height: 36px;
    }
    
    .slider-nav svg {
      width: 18px;
      height: 18px;
    }
  }
  
  @media (max-width: 480px) {
    .testimonials-section {
      padding: 3rem 0;
    }
    
    .section-title {
      font-size: 1.75rem;
      margin-bottom: 2rem;
    }
    
    blockquote {
      font-size: 1rem;
    }
  }
</style>
