---
---

<section class="offers-section" id="offers">
  <div class="offers-container">
    <h2 class="section-title">Nos solutions IA pour gagner du temps, simplifier votre gestion et renforcer votre positionnement</h2>
    
    <p class="section-intro">
      Que vous souhaitiez automatiser vos tâches, surveiller la concurrence ou améliorer votre visibilité en ligne, 
      nous avons une solution IA adaptée à votre PME. Découvrez nos packs clés en main ou demandez une solution sur mesure.
    </p>
    
    <div class="offers-grid">
      <div class="offer-card" data-pack="croissance">
        <div class="card-content">
          <div class="pack-icon">📈</div>
          <h3>Pack Croissance</h3>
          <p class="pack-description">Pour renforcer votre veille concurrentielle et rester toujours un temps d'avance.</p>
          
          <ul class="benefits-list">
            <li>Veille concurrentielle intelligente en temps réel</li>
            <li>Identification rapide des opportunités de marché</li>
            <li>Surveillance automatisée des tendances et nouveautés de votre secteur</li>
          </ul>
          
          <button class="cta-button">Découvrir ce pack</button>
        </div>
      </div>
      
      <div class="offer-card" data-pack="efficacite">
        <div class="special-offer-badge">Offre spéciale -30% jusqu'au 31/03</div>
        <div class="card-content">
          <div class="pack-icon">⚙️</div>
          <h3>Pack Efficacité</h3>
          <p class="pack-description">Pour gagner du temps et simplifier l'administratif.</p>
          
          <ul class="benefits-list">
            <li>Automatisation de vos tâches répétitives (emails, devis, factures)</li>
            <li>Gestion intelligente de vos documents (RH, comptabilité)</li>
            <li>Jusqu'à 15 heures gagnées par semaine</li>
          </ul>
          
          <button class="cta-button">Voir ce pack</button>
        </div>
      </div>
      
      <div class="offer-card" data-pack="visibilite">
        <div class="card-content">
          <div class="pack-icon">👀</div>
          <h3>Pack Visibilité</h3>
          <p class="pack-description">Pour améliorer votre présence en ligne.</p>
          
          <ul class="benefits-list">
            <li>SEO automatisé pour plus de visibilité sur Google</li>
            <li>Création automatique de contenus marketing</li>
            <li>Résumés d'actualité personnalisés</li>
          </ul>
          
          <button class="cta-button">En savoir plus</button>
        </div>
      </div>
    </div>
    
    <div class="custom-solution">
      <button class="secondary-cta">Besoin d'une solution spécifique ? Contactez-nous pour une offre sur mesure</button>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Animate cards on scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });
    
    // Observe each offer card
    document.querySelectorAll('.offer-card').forEach((card, index) => {
      // Add delay to stagger the animations
      card.style.transitionDelay = `${index * 0.1}s`;
      observer.observe(card);
    });
    
    // Add click event listeners to CTA buttons
    document.querySelectorAll('.cta-button').forEach(button => {
      button.addEventListener('click', () => {
        const pack = button.closest('.offer-card').dataset.pack;
        alert(`Vous avez sélectionné le ${pack.charAt(0).toUpperCase() + pack.slice(1)}. Nous vous redirigerons vers les détails de ce pack.`);
      });
    });
    
    // Add click event listener to secondary CTA
    document.querySelector('.secondary-cta').addEventListener('click', () => {
      alert('Nous allons vous rediriger vers notre formulaire de contact pour discuter de votre solution sur mesure.');
    });
  });
</script>

<style>
  .offers-section {
    padding: 6rem 2rem;
    background-color: #ffffff;
  }
  
  .offers-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .section-title {
    text-align: center;
    font-size: 2.2rem;
    line-height: 1.3;
    margin-bottom: 1.5rem;
    color: var(--text-color);
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .section-intro {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 3rem;
    color: #555;
    line-height: 1.6;
    font-size: 1.1rem;
  }
  
  .offers-grid {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }
  
  @media (min-width: 768px) {
    .offers-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (min-width: 1024px) {
    .offers-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  
  .offer-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    border: 1px solid #eaeaea;
    transition: all 0.3s ease;
    position: relative;
    opacity: 0;
    transform: translateY(20px);
    height: 100%;
    overflow: hidden;
  }
  
  .offer-card.visible {
    opacity: 1;
    transform: translateY(0);
  }
  
  .offer-card:hover {
    transform: scale(1.03);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
    border-color: var(--primary-color);
  }
  
  .card-content {
    padding: 2.5rem 2rem;
    height: 100%;
    display: flex;
    flex-direction: column;
  }
  
  .pack-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  
  .offer-card h3 {
    font-size: 1.8rem;
    margin-bottom: 1rem;
    color: var(--text-color);
  }
  
  .pack-description {
    color: #555;
    margin-bottom: 1.5rem;
    line-height: 1.5;
  }
  
  .benefits-list {
    list-style: none;
    margin-bottom: 2rem;
    flex-grow: 1;
  }
  
  .benefits-list li {
    padding: 0.7rem 0;
    border-bottom: 1px solid #f5f5f5;
    position: relative;
    padding-left: 1.8rem;
    color: #444;
  }
  
  .benefits-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--primary-color);
    font-weight: bold;
  }
  
  .cta-button {
    width: 100%;
    padding: 1rem;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: white;
    border: none;
    border-radius: 50px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: auto;
  }
  
  .cta-button:hover {
    transform: scale(1.05);
    box-shadow: 0 5px 15px rgba(58, 123, 213, 0.3);
  }
  
  .special-offer-badge {
    position: absolute;
    top: 1rem;
    right: -3rem;
    background: linear-gradient(135deg, #ff6b6b, #ff8e53);
    color: white;
    padding: 0.5rem 3rem;
    font-size: 0.85rem;
    font-weight: 600;
    transform: rotate(45deg);
    z-index: 10;
    box-shadow: 0 2px 10px rgba(255, 107, 107, 0.3);
  }
  
  .custom-solution {
    text-align: center;
    margin-top: 2rem;
  }
  
  .secondary-cta {
    background: transparent;
    border: 2px solid var(--primary-color);
    color: var(--primary-color);
    padding: 1rem 2rem;
    border-radius: 50px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-block;
  }
  
  .secondary-cta:hover {
    background-color: rgba(58, 123, 213, 0.1);
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(58, 123, 213, 0.15);
  }
  
  @media (max-width: 768px) {
    .offers-section {
      padding: 4rem 1.5rem;
    }
    
    .section-title {
      font-size: 1.8rem;
    }
    
    .card-content {
      padding: 2rem 1.5rem;
    }
  }
</style>
